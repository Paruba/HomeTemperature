@inject NavigationManager UriHelper

@code {
    [Parameter]
    public string UrlParameter { get; set; }

    protected override void OnAfterRender(bool firstRender)
    {
        base.OnAfterRender(firstRender);

        UriHelper.NavigateTo($"/{UrlParameter}");
    }
}